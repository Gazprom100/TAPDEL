require("dotenv").config(); const { MongoClient } = require("mongodb"); async function deleteFakeUsers() { const client = new MongoClient(process.env.MONGODB_URI); try { await client.connect(); const db = client.db("tapdel"); console.log("🧹 Удаляем тестовых пользователей..."); const result = await db.collection("users").deleteMany({ $or: [ { userId: "debug_withdrawal_user" }, { userId: "api_simple_test_user" }, { userId: "web-user-736284091" }, { userId: "web-user-139471841" }, { userId: "297810833" } ] }); console.log(`✅ Удалено ${result.deletedCount} тестовых пользователей`); const remainingUsers = await db.collection("users").find({}).toArray(); console.log(`📊 Осталось пользователей: ${remainingUsers.length}`); console.log("👥 Оставшиеся пользователи:"); remainingUsers.forEach(user => { console.log(`- ${user.userId} | ${user.profile?.username || "N/A"} | @${user.profile?.telegramUsername || "N/A"} | ${user.profile?.telegramId || "N/A"}`); }); } catch (error) { console.error("❌ Ошибка:", error); } finally { await client.close(); } } deleteFakeUsers();
