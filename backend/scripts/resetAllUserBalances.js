require("dotenv").config(); const { MongoClient } = require("mongodb"); async function resetAllUserBalances() { const client = new MongoClient(process.env.MONGODB_URI); try { await client.connect(); const db = client.db("tapdel"); console.log("🧹 Начинаем сброс всех балансов пользователей..."); const users = await db.collection("users").find({}).toArray(); console.log(`📊 Найдено пользователей: ${users.length}`); let resetCount = 0; for (const user of users) { try { const result = await db.collection("users").updateOne({ _id: user._id }, { \$set: { "gameState.tokens": 0, "gameState.highScore": 0 } }); if (result.modifiedCount > 0) { resetCount++; console.log(`✅ Сброшен баланс для ${user.userId} (${user.profile?.username || "N/A"})`); } await db.collection("user_token_balances").deleteMany({ userId: user.userId }); await db.collection("leaderboard").updateOne({ userId: user.userId }, { \$set: { score: 0, tokens: 0, highScore: 0 } }, { upsert: true }); } catch (error) { console.error(`❌ Ошибка сброса для ${user.userId}:`, error.message); } } console.log(`🎉 Сброс завершен! Сброшено: ${resetCount} пользователей`); } catch (error) { console.error("❌ Ошибка:", error); } finally { await client.close(); } } resetAllUserBalances();
