require("dotenv").config(); const { MongoClient } = require("mongodb"); async function cleanupFakeUsers() { const client = new MongoClient(process.env.MONGODB_URI); try { await client.connect(); const db = client.db("tapdel"); console.log("🧹 Начинаем очистку не настоящих пользователей..."); const result = await db.collection("users").deleteMany({ $or: [ { userId: { $regex: /^telegram-0+$/ } }, { userId: { $regex: /^telegram-1+$/ } }, { userId: { $regex: /^telegram-123+$/ } }, { userId: { $regex: /^fake-/i } }, { userId: { $regex: /^test/i } }, { userId: { $regex: /^demo/i } } ] }); console.log(`✅ Удалено ${result.deletedCount} не настоящих пользователей`); const totalUsers = await db.collection("users").countDocuments(); console.log(`📊 Всего пользователей: ${totalUsers}`); } catch (error) { console.error("❌ Ошибка:", error); } finally { await client.close(); } } cleanupFakeUsers();
